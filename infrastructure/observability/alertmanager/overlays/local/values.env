# owner: platform-observability
# spec: SPEC-OP-002
# rollback: git revert <sha>; recreate alertmanager
#
# LOCAL overlay parameters for Alertmanager. Consumed by observability-stack.yml.
# Environment parameters only — routing/receivers live in base/ (+ SPEC-OP-021).

ALERTMANAGER_STORAGE_PATH=/alertmanager
ALERTMANAGER_RETENTION=120h
