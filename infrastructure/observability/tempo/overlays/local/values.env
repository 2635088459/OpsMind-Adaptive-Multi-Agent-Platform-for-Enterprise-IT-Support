# owner: platform-observability
# spec: SPEC-OP-002
# rollback: git revert <sha>; recreate tempo
#
# LOCAL overlay parameters for Tempo. Consumed by observability-stack.yml
# (env_file; Tempo started with -config.expand-env=true). Environment parameters only.

# Laptop retention; production value is SPEC-OP-015.
TEMPO_BLOCK_RETENTION=24h
TEMPO_ENV_LABEL=local
