{
  "_fixture": { "spec": "SPEC-OP-028", "kind": "conformant", "expect": "pass",
    "note": "evaluation-improvement-service's real EvaluationTelemetry (SPEC-EI-033) case-result counter. Replaces this namespace's original example content, which grep confirmed was never emitted anywhere in this codebase (SPEC-OP-028's own traceability doc records the correction)." },
  "metric": {
    "name": "evaluation_case_total",
    "type": "counter", "unit": "1", "namespace": "evaluation",
    "labels": ["result", "criticality"]
  }
}
