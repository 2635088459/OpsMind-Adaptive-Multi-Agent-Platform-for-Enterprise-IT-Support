{
  "_fixture": { "spec": "SPEC-OP-006", "kind": "conformant", "expect": "pass",
    "note": "agent tool-call counter; labels are all bounded." },
  "metric": {
    "name": "agent_tool_calls_total",
    "type": "counter", "unit": "1", "namespace": "agent",
    "labels": ["agent_role", "model", "outcome"]
  }
}
