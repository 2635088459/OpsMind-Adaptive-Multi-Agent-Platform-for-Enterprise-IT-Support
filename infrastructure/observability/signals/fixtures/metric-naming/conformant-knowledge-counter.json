{
  "_fixture": { "spec": "SPEC-OP-026", "kind": "conformant", "expect": "pass",
    "note": "memory-knowledge-service's real MemoryTelemetry embedding-failure counter — a distinct `knowledge_` prefix from the same class's other `memory_*` metrics." },
  "metric": {
    "name": "knowledge_embedding_failure_total",
    "type": "counter", "unit": "1", "namespace": "knowledge",
    "labels": ["context"]
  }
}
