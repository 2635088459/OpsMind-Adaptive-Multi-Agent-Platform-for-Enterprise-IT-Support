{
  "_fixture": { "spec": "SPEC-OP-027", "kind": "nonconformant", "expect": "reject",
    "reject_reason": "execution_id is an unbounded per-attempt identifier — forbidden in the tool namespace exactly as ToolGatewayTelemetry.py's own module docstring already states (\"never an id (toolRequestId, executionId, resultEnvelopeId)\")." },
  "metric": {
    "name": "tool_execution_latency_seconds",
    "type": "histogram", "unit": "seconds", "namespace": "tool",
    "labels": ["connector", "execution_id"]
  }
}
