{
  "_fixture": { "spec": "SPEC-OP-027", "kind": "conformant", "expect": "pass",
    "note": "proves the _count gauge suffix (added this spec) actually works — tool-integration-gateway's own tool_outbox_pending_count." },
  "metric": {
    "name": "tool_outbox_pending_count",
    "type": "gauge", "unit": "1", "namespace": "tool",
    "labels": []
  }
}
