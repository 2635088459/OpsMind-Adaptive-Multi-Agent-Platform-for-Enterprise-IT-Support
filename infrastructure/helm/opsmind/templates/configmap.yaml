apiVersion: v1
kind: ConfigMap
metadata:
  name: opsmind-shared-config
  labels:
    {{- include "opsmind.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
