# SPEC-ARO-038 — 领域规则

目标：支撑 `发起会话即建单`。

- 建单和创建 workflow instance 是两次真实、独立的调用（一次跨服务 HTTP 调用，再一次本地领域命令）——从不合并成一个假的原子步骤。
- 建单成功但随后创建 workflow instance 失败时，本 spec 不尝试补偿性地取消工单——工单依然是一条真实、有效的记录，人工/support-console 路径仍能接手处理。
- 请求方身份（JWT `sub`）被记录为工单的真实请求人——从不用一个服务账号身份代替员工本人。
