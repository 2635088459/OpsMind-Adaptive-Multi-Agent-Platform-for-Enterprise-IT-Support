# SPEC-ARO-043 — Test Plan

目标：支撑 `外呼服务身份`。

- 针对真实 Keycloak realm 的集成测试（复用 2026-09-01 集成验证时搭建的那个），断言真实获取到 token 且被下游真实端点接受。
- 失败测试：Keycloak 临时不可用——外呼干净失败并给出明确错误，从不悄悄以未鉴权方式绕过。
- 刷新测试：临近过期的 token 被透明刷新，不对调用方暴露可见的失败。
- 一次静态/日志扫描检查，确认任何密钥值都不出现在任何日志行或 trace span 里。
