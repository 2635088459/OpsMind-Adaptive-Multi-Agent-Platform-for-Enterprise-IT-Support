# SPEC-ARO-043 — Persistence Design

目标：支撑 `外呼服务身份`。

- 没有数据库表。token 只在内存中缓存（短生命周期，过期前刷新）——从不持久化到 Postgres 或任何持久存储。
- 客户端密钥只存在于环境配置里（遵循本项目已经确立的密钥处理约定，例如本平台其他地方已经在用的 `KEYCLOAK_CLIENT_SECRET` 式注入）——从不出现在提交进源码的配置文件里。
