# SPEC-ARO-041 — 领域规则

目标：支撑 `借助既有分诊转人工`。

- 转人工是工作流实例的终态事件，不是可暂停/恢复的——一旦被分诊出去，同一个工作流实例永远不再对该工单重新进入自动化。
- agent 可能推断出的分类/团队路由提示（比如"VPN"、"硬件"）作为建议传给分诊调用，但真正的路由决定（队列/团队分配）是 `02-ticket-workflow` 自己的，本 spec 不覆盖它。
