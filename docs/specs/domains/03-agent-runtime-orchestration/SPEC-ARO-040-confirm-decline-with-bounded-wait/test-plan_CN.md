# SPEC-ARO-040 — Test Plan

目标：支撑 `确认/拒绝与限时同步等待`。

- 集成测试：完整跑通 `05-tool-integration-gateway`，断言快速工具场景下限时等待真的能解析为 `"done"`。
- 超时路径测试：模拟一个慢工具，在限时等待之后才完成——断言返回 `"still-processing"`，且真正完成后依然被正确记录（不丢失）。
- 集成测试：高风险路径真实创建一条治理审批请求，复用 2026-09-01 集成验证里已经证明可行的那条链路。
- 幂等测试：对同一个 `actionId` 并发重复提交确认/拒绝请求，只产生一次真实副作用。
