# SPEC-TW-015 — 事件契约

消费：`approval.granted.v1`。

发布：`ticket.approval-granted-applied.v1`。

发布 payload 包含 `approvalId`、`approvalRequestId`、`workflowId`、`actionId`、`approvedAt`、`authorizationReference`。
