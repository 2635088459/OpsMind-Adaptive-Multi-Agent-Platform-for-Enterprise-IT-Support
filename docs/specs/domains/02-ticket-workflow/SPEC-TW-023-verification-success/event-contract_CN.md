# SPEC-TW-023 — 事件契约

消费：`verification.completed.v1`。

发布：`ticket.verification-success-applied.v1`。

Payload 包含 `verificationId`、`verificationEvidenceId`、`workflowId`、`resolutionCycleId`、`attemptNumber`、`completedAt`。
