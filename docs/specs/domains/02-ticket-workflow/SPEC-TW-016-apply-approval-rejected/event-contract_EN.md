# SPEC-TW-016 — Event Contract

Consumes: `approval.rejected.v1`.

Publishes: `ticket.approval-rejected-applied.v1`.

Payload includes approval/action/workflow references, `rejectedAt`, and sanitized reason code.
