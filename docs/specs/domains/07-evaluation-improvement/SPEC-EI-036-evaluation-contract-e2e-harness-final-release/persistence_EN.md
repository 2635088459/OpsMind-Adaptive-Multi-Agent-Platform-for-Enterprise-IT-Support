# Persistence Design — SPEC-EI-036

The spec writes only to the `evaluation.*` schema, preserving idempotency keys, versions, hashes, source references, audit records, outbox events, and redacted evidence references.

See `persistence_CN.md` for full detail.
