# Test Plan — SPEC-EI-029

Tests must cover domain invariants, persistence constraints, API/event contracts, security, redaction, idempotency, outbox replay, dependency failures, and spec-specific happy/failure paths.

See `test-plan_CN.md` for full detail.
