# 07 Data Model

This section defines the `evaluation.*` PostgreSQL schema, tables, unique keys, artifact references, outbox, processed events, and audit records.

The authoritative design for this first pass is in `README_CN.md`.

