# 02 Business Invariants

This section defines the non-bypassable evaluation and controlled-improvement invariants, including release gates, dataset immutability, deterministic safety graders, and candidate promotion constraints.

The authoritative design for this first pass is in `README_CN.md`.

