# 04 Use Cases

This section defines the main workflows for golden dataset creation, offline benchmark execution, release gate evaluation, improvement candidate generation, approval, canary, rollback, and sampled online evaluation.

The authoritative design for this first pass is in `README_CN.md`.

