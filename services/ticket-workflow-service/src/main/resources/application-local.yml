spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:ticket_workflow}
    username: ${DB_USERNAME:ticket_workflow}
    password: ${DB_PASSWORD:ticket_workflow}
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:guest}
    password: ${RABBITMQ_PASSWORD:guest}
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8081/realms/opsmind}

opsmind:
  ticket:
    requester-pseudonymization-secret: ${TICKET_REQUESTER_PSEUDONYMIZATION_SECRET:opsmind-local-dev-secret-change-me}
    list-cursor-signing-secret: ${TICKET_LIST_CURSOR_SIGNING_SECRET:opsmind-local-dev-cursor-secret-change-me}

management:
  otlp:
    metrics:
      export:
        url: ${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4318}/v1/metrics
    tracing:
      endpoint: ${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4318}/v1/traces

logging:
  level:
    dev.opsmind.ticketworkflow: DEBUG
