"""Adapters: Protocol implementations. Depend on ``tool_gateway.ports`` and
``tool_gateway.domain``, imported only by ``tool_gateway.container`` (the
composition root) — 13-package-and-class-design §"Dependency Direction":
"adapters implement ports."
"""

from __future__ import annotations
